{
  "name": "api",
  "version": "1.0.0",
  "description": "A simple backend api for a POC nextjs project.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "docker:build": "docker build -t alexlapinski/camera-poc-api .",
    "docker:run": "docker run -p $PORT:$PORT alexlapinski/camera-poc-api ."
  },
  "author": "Alex Lapinski <contact@alexlapinski.name>",
  "license": "MIT",
  "dependencies": {
    "express": "^4.16.3",
    "express-validator": "^6.0.0",
    "redis": "^2.8.0"
  }
}
